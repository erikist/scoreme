$("#pageCarousel").carousel(1);
$("#yield-container").fadeOut(500, function()
{
	var newContent = "<%= escape_javascript(yield) %>";
	$("#yield-container").html(newContent);
	$("#yield-container").fadeIn(500);
});